node_modules/
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.DS_Store
*.local

# electron desktop build output
dist-desktop/

# local decompile/installer working dirs — never publish (RE source material)
axe3/
fm9/

# Local-only Claude Code context (documents RE/capture methods — never commit)
# (root-anchored: src/lib/axis-workbench/CLAUDE.md is a committable framework guide)
/CLAUDE.md

# Design reference screenshots (large PNGs, not needed in the repo)
design/screenshots/

# Local Workbench visual smoke screenshots
.workbench-smoke/

# Playwright artifacts
/test-results
/playwright-report

# Claude Code local config (private: Plane MCP, personal settings)
.mcp.json
CLAUDE.local.md
.claude/settings.local.json

# Explicitly local design notes (surfaced when /CLAUDE.md was root-anchored)
/design/CLAUDE.md

# stack-worktree.sh parallel-work worktrees
.wt-*/
